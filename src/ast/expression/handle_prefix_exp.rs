use std::sync::Arc;

use tree_sitter::Node;

use crate::prelude::{Expression, ExpressionInner, SingleToken};

pub fn handle_prefix_exp(prefix_exp: Node, code_bytes: &[u8]) -> ExpressionInner {
    todo!()
}
